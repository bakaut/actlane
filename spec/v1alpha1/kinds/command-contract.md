# CommandContract

`CommandContract` describes a user-facing command entrypoint for an Actlane capability.

The contract owns invocation metadata and prompt text for a target command file. Capability semantics, policy rules, MCP bindings, and runtime execution remain in their own contracts.
