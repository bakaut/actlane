# AdoptionProfile

`AdoptionProfile` describes how Actlane behaves in an existing project.

Default posture:

```text
never overwrite
require explicit apply
write ownership markers
record lockfile ownership
remove only owned files and blocks
```

This kind is central to brownfield-safe adoption.
