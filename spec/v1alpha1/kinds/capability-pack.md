# CapabilityPack

`CapabilityPack` groups related capabilities, policies, targets, examples, and generated artifacts.

The first pack is:

```text
safe-gitops
```

The pack should be inspectable before installation and reproducible through a lockfile.
