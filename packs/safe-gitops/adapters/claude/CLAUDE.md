# Claude Instructions For safe-gitops

Use `create-safe-draft-pr` as a safe draft PR workflow.

Rules:

- Ask for explicit confirmation before repository mutations.
- Do not touch secret or credential files.
- Keep pull requests as draft.
- Use branch prefix `actlane/`.
- Return policy decision, changed files, and PR URL.

Status: Phase 0 expected artifact, not generated by a CLI yet.
